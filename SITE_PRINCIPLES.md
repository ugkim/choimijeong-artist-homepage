# 다국어 정적 사이트 구축 원칙

이 파일은 새 사이트를 시작할 때 AI와 개발자가 가장 먼저 읽고 따라야 하는 필수 설계 규칙이다. `site-bootstrap.config.json`과 함께 새 저장소 루트에 복사한다. 프로젝트별 요구사항이 이 문서와 충돌하면 변경 이유와 마이그레이션 방법을 먼저 기록하고 승인받는다.

## 1. 콘텐츠의 단일 원본

1. 기본 언어 파일이 사이트 전체 콘텐츠의 유일한 원본(source of truth)이다.
2. 기본 언어는 한국어로 고정하지 않고 `site-bootstrap.config.json`의 `defaultLocale`로 지정한다.
3. 공통 데이터와 기본 언어 텍스트를 다른 관리 화면에 분리하지 않는다. 작품 하나를 편집할 때 이미지, 연도, 제목, 재료, 크기, 설명을 한곳에서 입력한다.
4. 고유 ID는 생성 후 변경하지 않는다. 번역, 관계 참조, URL 생성은 이 ID를 기준으로 한다.
5. 선택 기능은 상위 객체와 모든 하위 입력을 선택사항으로 만들고 값이 없으면 빈 객체가 아니라 `null`을 저장한다.

## 2. 번역 데이터 규칙

1. 언어 파일은 `content/data/{언어코드}.json` 형식을 사용한다.
2. 모든 번역 파일은 기본 언어 파일과 객체 키, 중첩 단계, 자료형이 정확히 같아야 한다.
3. ID가 있는 배열은 순서가 아니라 ID 집합을 비교한다. 기본 언어에 있는 ID가 번역 파일에 빠지거나 추가되면 검증 실패다.
4. ID가 없는 배열은 길이와 각 위치의 구조가 같아야 한다.
5. 이미지, 날짜, 공개 여부, 순서 등 번역하지 않는 값도 번역 파일에 같은 키로 포함한다. 생성 도구가 기본 언어 값을 복제하며 사람이 다시 입력하게 하지 않는다.
6. 번역문이 없으면 키를 삭제하지 말고 빈 문자열을 사용한다. 운영 정책에 따라 기본 언어로 대체하거나 배포를 막는다.
7. JSON 문법과 구조 검증을 통과하지 못한 콘텐츠는 빌드·배포하지 않는다.

## 3. 가져오기·내보내기

1. 원본 JSON은 특정 CMS에 종속되지 않는 표준 데이터여야 한다.
2. 빌드 전에 모든 언어를 JSON과 UTF-8 BOM CSV로 내보낸다.
3. CSV는 `key,value` 두 열을 사용하고 배열 ID는 `$.[collection][id=...]` 경로에 보존한다.
4. 번역 작업 순서는 `기본 언어 JSON/CSV 내보내기 → 번역 → 같은 키로 가져오기 → 검증 → 배포`다.
5. CMS 저장, 로컬 빌드, CI 빌드는 모두 같은 검증 명령을 사용한다.

## 4. 관리자 UX

1. 기본 언어 전체 데이터를 첫 번째 관리 항목으로 제공한다.
2. 언어별 번역 파일은 언어당 하나의 전체 데이터 항목으로 제공한다. 작품 번역, 카테고리 번역, CV 번역처럼 기능별 번역 메뉴를 흩어 놓지 않는다.
3. 혼자 운영하는 사이트에는 승인 워크플로를 기본 활성화하지 않는다. 실제로 초안·검수 역할이 필요할 때만 켠다.
4. 고급 설정과 선택 기능은 기본적으로 접어 둔다.
5. 관리자가 내부 ID를 직접 입력해야 하는 관계 필드는 가능한 한 검색·선택 UI로 제공한다.
6. CMS가 원본 파일을 소유하지 않는다. CMS는 표준 JSON을 편집하는 교체 가능한 도구다.

## 5. 검증·배포 게이트

배포 전에 JSON 파싱, 재귀 키 구조 비교, 배열 ID 집합 및 자료형 비교, 도메인 검증, 타입 검사, 정적 빌드, JSON/CSV 산출물 확인을 순서대로 통과한다.

이 저장소의 기준 명령은 `npm run validate`, `npm run export:content`, `npm run generate`다. 패키지 관리자가 달라도 같은 스크립트 이름을 유지한다.

## 6. GitHub·Netlify 자동화 원칙

1. AI는 코드 작성, 검증, Git 초기화, 저장소 생성, push, Netlify 프로젝트 생성, Git 연동, 환경 변수와 빌드 설정, 첫 배포까지 자동화할 수 있다.
2. GitHub·Netlify 로그인, OAuth 권한 승인, 결제 또는 조직 정책 승인은 계정 소유자가 수행한다. AI는 승인 이후 CLI/API 작업을 계속한다.
3. 토큰과 비밀번호는 저장소에 기록하지 않고 로컬 보안 저장소나 CI 환경 변수만 사용한다.
4. 배포는 `netlify.toml`을 기준으로 하며 대시보드 수동 설정은 꼭 필요한 인증·도메인 작업으로 제한한다.
5. Identity 초대 링크는 `/admin/`에서 토큰을 처리한다. 이메일 템플릿을 쓸 수 없는 요금제라면 루트 콜백이 토큰을 보존해 `/admin/`으로 전달한다.
6. 새 프로젝트에서 Decap CMS 인증 방식을 선택할 때 Git Gateway를 무조건 기본값으로 삼지 않는다. 현재 지원 상태와 운영자의 GitHub 계정 보유 여부를 확인해 GitHub OAuth, Netlify Identity 또는 다른 CMS를 결정한다.

## 7. AI에게 주는 최초 작업 지시문

> `SITE_PRINCIPLES.md`와 `site-bootstrap.config.json`을 먼저 읽고 필수 조건으로 적용하라. 기본 언어 파일을 완전한 단일 원본으로 만들고 모든 번역 파일이 동일한 키 구조와 ID 집합을 갖도록 생성·검증하라. JSON/CSV 내보내기와 배포 전 검증을 구현하라. 관리자 화면에서는 기본 언어의 공통 정보와 텍스트를 한곳에서 편집하게 하고 번역 메뉴를 기능별로 분산하지 마라. GitHub와 Netlify 작업은 CLI/API로 자동화하되 로그인·OAuth 승인과 비밀정보 입력이 필요할 때만 사용자에게 요청하라. 완료 전 검증, 타입 검사, 프로덕션 빌드를 실행하고 결과를 보고하라.
